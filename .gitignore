# Environment / secrets
.env
.env.*
*.local
secrets.json
credentials.json

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / editor cruft
.DS_Store
.DS_Store?
._*
Thumbs.db
ehthumbs.db
Desktop.ini
*.swp
*.swo
*~
.vscode/
.idea/
.claude/

# Dependencies / build output
node_modules/
dist/
build/
.next/
.vercel/
.cache/

# Python scratchpads (if any)
__pycache__/
*.py[cod]
.venv/
venv/

# Raw photo originals at repo root — site uses optimized copies in web/ instead.
# Keep local copies on disk, but don't commit them (they're ~3 GB).
/*.jpg
/*.JPG
/*.jpeg
/*.JPEG
# (Allow the pngtree moon favicon explicitly — it IS served)
!pngtree-*.png

# Scratch / superseded data files
photos.json
photos2.js
photos22.js

# Local meta-doc tracking files added by John to this fork (not for upstream)
NEW_FILES_LOCAL.md

# tools/import/ run output — state, queue, staged files, logs
# (source files in tools/import/ ARE tracked; only run-output is ignored)
tools/import/state.json
tools/import/inbox.json
tools/import/staging/
tools/import/log/

# Source reference materials (not part of the site)
artemis-ii-overview-timeline-public-final.pdf
artemis-ii-timeline.jsx
2026-04-10-post-rtc3-to-ei.zip
2026.04.10 - Post-RTC3 to EI
